<layout>

    <data>
        <variable
                name="item"
                type="com.tistory.black_jin0427.navermovie.api.model.MovieItem"/>

        <variable
                name="listener"
                type="com.tistory.black_jin0427.navermovie.adapter.MainAdapter.OnItemClickListener"/>
    </data>

    <androidx.cardview.widget.CardView
            xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:tools="http://schemas.android.com/tools"
            xmlns:app="http://schemas.android.com/apk/res-auto"

            app:cardElevation="5dp"
            android:layout_margin="10dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:foreground="?android:attr/selectableItemBackground">

        <LinearLayout
                android:padding="20dp"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:layout_gravity="center"
                android:orientation="vertical"
                android:onClick="@{() -> listener.onClick(item)}">

            <ImageView
                    android:id="@+id/iv_item_movie"
                    android:layout_width="100dp"
                    android:layout_height="100dp"
                    android:layout_gravity="center"
                    android:url="@{item.image}"
                    tools:background="@drawable/ic_launcher_background"/>

            <TextView
                    android:id="@+id/tv_item_movie_title"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center"
                    android:layout_marginTop="10dp"
                    android:textColor="@color/colorAccent"
                    android:textSize="17sp"
                    android:textStyle="bold"
                    android:htmlText="@{item.title}"
                    tools:text="Black Jin"/>

            <TextView
                    android:id="@+id/tv_item_movie_sub_title"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="20dp"
                    android:textColor="@android:color/black"
                    android:textSize="15sp"
                    android:text="@{item.subtitle}"
                    tools:text="tistory"/>

            <TextView
                    android:id="@+id/tv_item_movie_link"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:textColor="@android:color/black"
                    android:textSize="15sp"
                    android:text="@{item.link}"
                    tools:text="black-jin0427.tistory.com"/>

            <TextView
                    android:id="@+id/tv_item_movie_actor"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:textColor="@color/colorAccent"
                    android:textSize="15sp"
                    android:text="@{item.actor}"
                    tools:text="actor"/>

        </LinearLayout>

    </androidx.cardview.widget.CardView>

</layout>